import { request } from "./request";
import { setToken, getToken, removeToken } from "./token";
import { setUserInfo, getUserInfo, removeUserInfo } from "./userInfo";

export {
  request,
  setToken,
  getToken,
  removeToken,
  setUserInfo,
  getUserInfo,
  removeUserInfo,
};
